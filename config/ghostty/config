# =============================================================================
# Ghostty Config (migrated from Alacritty)
# =============================================================================

# Theme
# theme = Catppuccin Macchiato
theme = Catppuccin Frappe

# Font
font-family = HackGen35 Console NF
font-size = 14
font-thicken = true
font-feature = -calt

# Window
background-opacity = 0.8
window-padding-x = 10
window-padding-y = 10

# macOS: Option key as Alt (Alacritty の option_as_alt = "Both" に相当)
macos-option-as-alt = true

# Terminal type (tmux互換)
term = xterm-256color

# Shell: tmuxセッションに自動アタッチ、なければ新規作成
# tmux はPATHから解決する (macOS Apple Silicon の絶対パス決め打ちを避ける)
command = /bin/zsh -l -c "tmux new-session -A -s main"
